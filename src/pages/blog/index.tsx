import { Default } from "@/components/layouts/Default";
import type { NextPage } from "next";

const Page: NextPage = () => {
  return (
    <Default>
      <div />
    </Default>
  );
};

export default Page;
